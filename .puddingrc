#!/usr/bin/env bash
# -*-coding:raw-text-unix;-*-
#
# .puddingrc -- Project resource file
#
# The purpose of this file is to set global variables based on the
# current value of
#
#    $TestFlavor
#
# The value is set to -c NAME argument passed to "pudding.sh"
#
TestTarget='Libpreempt'

case ${TestFlavor}
in
  std)
    Tests=($(find_tests $TestDir 'std_*.cpp'))
    ;;
  pthread)
    Tests=($(find_tests $TestDir 'pthread_*.cpp'))
    ;;
  task)
    Tests=($(find_tests $TestDir 'task_*.cpp'))
    ;;
  process)
    Tests=($(find_tests $TestDir 'process_*.cpp'))
    ;;
  sched)
    Tests=($(find_tests $TestDir 'sched_*.cpp'))
    ;;
  parinama)
    #
    # use this when developing/debugging a single test
    #
    # this is a Sanskrit word and means development, change
    #
    Tests=($(find_tests $TestDir 'new_thing.cpp'))
    ;;
esac

