/* -*- coding: raw-text-unix; -*-
 *
 * Base library main function.
 */
#include <base/all.h>

int main(int argc, char *argv[])
{
  // your code

  return global_verify_flag() ? EXIT_SUCCESS : EXIT_FAILURE;
}
